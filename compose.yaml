# Thin wrapper so `docker compose config` from this directory finds
# the bayt-generated graph instead of walking up to the repo root
# (which exposes a root-level `integrate` service that runs a totally
# unrelated act-based CI workflow). Same shape as services/tracker's
# compose.yaml.
include:
  - path: ./.bayt/compose.yaml
