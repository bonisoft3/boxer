# generated from bayt.cue — do not edit
services:
  services_boxer-build:
    build:
      additional_contexts:
        services_boxer-setup: service:services_boxer-setup
      context: ..
      dockerfile: .bayt/Dockerfile.build
      target: build
      x-bake:
        cache-from:
        - type=registry,ref=ghcr.io/worldsense/trash/boxer-cache:v1-${CACHE_SCOPE:-unscoped}-build
        - type=registry,ref=ghcr.io/worldsense/trash/boxer-cache:v1-${CACHE_SCOPE_FALLBACK:-unscoped}-build
        cache-to:
        - type=registry,ref=ghcr.io/worldsense/trash/boxer-cache:v1-${CACHE_SCOPE:-unscoped}-build,mode=min,image-manifest=true,oci-mediatypes=true
    image: bayt-services_boxer-build:${BAYT_IMAGE_TAG:-latest}
