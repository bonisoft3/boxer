# generated from bayt.cue — do not edit
services:
  services_boxer-setup:
    build:
      context: ..
      dockerfile: .bayt/Dockerfile.setup
      target: setup
      x-bake:
        cache-from:
        - type=registry,ref=ghcr.io/worldsense/trash/boxer-cache:v1-${CACHE_SCOPE:-unscoped}-setup
        - type=registry,ref=ghcr.io/worldsense/trash/boxer-cache:v1-${CACHE_SCOPE_FALLBACK:-unscoped}-setup
        cache-to:
        - type=registry,ref=ghcr.io/worldsense/trash/boxer-cache:v1-${CACHE_SCOPE:-unscoped}-setup,mode=min,image-manifest=true,oci-mediatypes=true
    image: bayt-services_boxer-setup:${BAYT_IMAGE_TAG:-latest}
    scale: 0
